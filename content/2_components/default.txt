Title: Components

----

Content:

Reusable JavaScript logic is encapsulated in components via ES6 classes. In its constructor function, each component accepts the following arguments:

- `element` to which the component is attached to
- `options` that alter the component's behavior
- `parent` component, if any